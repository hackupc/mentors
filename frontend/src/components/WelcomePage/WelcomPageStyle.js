import { colors, globalCard,  } from '../Styles'
export { styles }

const styles = theme => ({
    card: globalCard,
    link: {
        color: colors.primary.dark
    },
    text: {
        fontWeight: 'normal',
        textAlign: 'center'
    }
});