import { globalCard, globalButton, titleCard, snackbar } from '../Styles'
export {styles}

const styles = theme => ({
    card: globalCard,
    button: globalButton,
    snackbar: snackbar,
    title: titleCard,
})