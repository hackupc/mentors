import { globalCard, globalButton } from '../Styles'
export {styles}

const styles = theme => ({
    card: globalCard,
    button: globalButton,
    snackbar: {
        marginBottom: 20
    },
    title: {
        textAlign: 'Left',
        fontWeight: 'normal'
    },
})